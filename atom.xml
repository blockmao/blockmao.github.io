<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://blockmao.github.io</id>
    <title>黑猫科技博客</title>
    <updated>2020-04-21T16:19:36.833Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://blockmao.github.io"/>
    <link rel="self" href="https://blockmao.github.io/atom.xml"/>
    <logo>https://blockmao.github.io/images/avatar.png</logo>
    <icon>https://blockmao.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, 黑猫科技博客</rights>
    <entry>
        <title type="html"><![CDATA[MySQL：索引详解]]></title>
        <id>https://blockmao.github.io/zFL4EV3Km/</id>
        <link href="https://blockmao.github.io/zFL4EV3Km/">
        </link>
        <updated>2020-04-12T10:45:07.000Z</updated>
        <summary type="html"><![CDATA[<ol>
<li>什么索引？</li>
<li>索引底层数据结构</li>
<li>建立索引的原则</li>
<li>慢查询优化</li>
</ol>
]]></summary>
        <content type="html"><![CDATA[<ol>
<li>什么索引？</li>
<li>索引底层数据结构</li>
<li>建立索引的原则</li>
<li>慢查询优化</li>
</ol>
<!-- more -->
<h2 id="原文地址">原文地址</h2>
<ol>
<li>MySQL索引原理及慢查询优化: https://tech.meituan.com/2014/06/30/mysql-index.html</li>
<li>Explain详解: https://blog.csdn.net/qq_38975553/article/details/104134587</li>
<li>BTree和B+Tree详解: https://blog.csdn.net/weixin_41948075/article/details/100180136?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522158748222519725211965459%2522%252C%2522scm%2522%253A%252220140713.130102334.pc%255Fblog.%2522%257D&amp;request_id=158748222519725211965459&amp;biz_id=0&amp;utm_source=distribute.pc_search_result.none-task-blog-2<sub>blog</sub>first_rank_v2~rank_v25-1</li>
</ol>
<h2 id="索引概述">索引概述</h2>
<p><strong>定义</strong>：<strong>索引是存储引擎用于快速找到记录的一种数据结构</strong>。举例说明：如果查找一本书中的某个特定主题，一般会先看书的目录（类似索引），找到对应页面。在MySQL，存储引擎采用类似的方法使用索引，高效获取查找的数据。</p>
<p><strong>索引的分类</strong></p>
<p>1）从存储结构上来划分</p>
<ul>
<li>Btree 索引（B+tree，B-tree)</li>
<li>哈希索引</li>
<li>full-index 全文索引</li>
</ul>
<p>2）从应用层次上来划分</p>
<ul>
<li>普通索引：即一个索引只包含单个列，一个表可以有多个单列索引。</li>
<li>唯一索引：索引列的值必须唯一，但允许有空值。</li>
<li>复合索引：一个索引包含多个列。</li>
</ul>
<p>3）从表记录的排列顺序和索引的排列顺序是否一致来划分</p>
<ul>
<li>聚集索引：表记录的排列顺序和索引的排列顺序一致。</li>
<li>非聚集索引：表记录的排列顺序和索引的排列顺序不一致。</li>
</ul>
<h2 id="索引底层数据结构">索引底层数据结构</h2>
<h3 id="磁盘io与预读">磁盘IO与预读</h3>
<p>数据库保存的数据是存储在磁盘上，查找数据时需要将磁盘中的数据加载到内存中，在介绍索引的实现之前，先了解下磁盘IO与预读。</p>
<p>磁盘读取数据靠的是机械运动，每次读取数据花费的时间可以分为寻道时间、旋转延迟、传输时间三个部分，寻道时间指的是磁臂移动到指定磁道所需要的时间，主流磁盘一般在5ms以下；旋转延迟就是我们经常听说的磁盘转速，比如一个磁盘7200转，表示每分钟能转7200次，也就是说1秒钟能转120次，旋转延迟就是1/120/2 = 4. 17ms；传输时间指的是从磁盘读出或将数据写入磁盘的时间，一般在零点几毫秒，相对于前两个时间可以忽略不计。那么访问一次磁盘的时间，即一次磁盘IO的时间约等于5+4. 17 = 9ms左右，听起来还挺不错的，但要知道一台500 -MIPS的机器每秒可以执行5亿条指令，因为指令依靠的是电的性质，换句话说执行一次IO的时间可以执行40万条指令，数据库动辄十万百万乃至千万级数据，每次9毫秒的时间，显然是个灾难。</p>
<p>下图是计算机硬件延迟的对比图，供大家参考：</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d73daa0543c8?w=548&amp;h=321&amp;f=png&amp;s=22767" alt="various-system-software-hardware-latencies" loading="lazy"></figure>
<p>考虑到磁盘IO是非常高昂的操作，计算机操作系统做了一些优化，当一次IO时，不光把当前磁盘地址的数据，而是把相邻的数据也都读取到内存缓冲区内，因为局部预读性原理告诉我们，当计算机访问一个地址的数据的时候，与其相邻的数据也会很快被访问到。每一次IO读取的数据我们称之为一页(page)。具体一页有多大数据跟操作系统有关，一般为4k或8k，也就是我们读取一页内的数据时候，实际上才发生了一次IO，这个理论对于索引的数据结构设计非常有帮助。</p>
<h3 id="b-tree和btree">B-Tree和B+Tree</h3>
<h4 id="b-tree">B-tree</h4>
<p>B-Tree是为磁盘等外存储设备设计的一种平衡查找树。</p>
<p>B-Tree结构的数据可以让系统高效的找到数据所在的磁盘块。为了描述B-Tree，首先定义一条记录为一个二元组[key, data] ，key为记录的键值，对应表中的主键值，data为一行记录中除主键外的数据。对于不同的记录，key值互不相同。</p>
<p>一棵m阶的B-Tree有如下特性：</p>
<ol>
<li>每一个节点最多有 <em>m</em> 个子节点</li>
<li>每一个非叶子节点（除根节点）最少有 ⌈<em>m</em>/2⌉ 个子节点</li>
<li>如果根节点不是叶子节点，那么它至少有两个子节点</li>
<li>有 <em>k</em> 个子节点的非叶子节点拥有 <em>k</em> − 1 个键</li>
<li>所有的叶子节点都在同一层</li>
</ol>
<p>B-Tree中的每个节点根据实际情况可以包含大量的关键字信息和分支，如下图所示为一个3阶的B-Tree：</p>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d740dcac65bb?w=894&amp;h=298&amp;f=png&amp;s=37191" alt="B-Tree" loading="lazy"></figure>
<p>每个节点占用一个盘块的磁盘空间，一个节点上有两个升序排序的关键字和三个指向子树根节点的指针，指针存储的是子节点所在磁盘块的地址。两个关键词划分成的三个范围域对应三个指针指向的子树的数据的范围域。以根节点为例，关键字为17和35，P1指针指向的子树的数据范围为小于17，P2指针指向的子树的数据范围为17~35，P3指针指向的子树的数据范围为大于35。</p>
<p>模拟查找关键字29的过程：</p>
<ol>
<li>
<p>根据根节点找到磁盘块1，读入内存。【磁盘I/O操作第1次】</p>
<p>比较关键字29在区间（17, 35），找到磁盘块1的指针P2。</p>
</li>
<li>
<p>根据P2指针找到磁盘块3，读入内存。【磁盘I/O操作第2次】</p>
<p>比较关键字29在区间（26, 30），找到磁盘块3的指针P2。</p>
</li>
<li>
<p>根据P2指针找到磁盘块8，读入内存。【磁盘I/O操作第3次】</p>
<p>在磁盘块8中的关键字列表中找到关键字29。</p>
</li>
</ol>
<p>分析上面过程，发现需要3次磁盘I/O操作，和3次内存查找操作。由于内存中的关键字是一个有序表结构，可以利用二分法查找提高效率。而3次磁盘I/O操作是影响整个B-Tree查找效率的决定因素。B-Tree相对于AVLTree缩减了节点个数，使每次磁盘I/O取到内存的数据都发挥了作用，从而提高了查询效率。</p>
<h4 id="btree">B+Tree</h4>
<p>B+Tree是在B-Tree基础上的一种优化，InnoDB存储引擎就是用B+Tree实现其索引结构。</p>
<p>在B+Tree中，所有数据记录节点都是按照键值大小顺序存放在同一层的叶子节点上，而非叶子节点上只存储key值信息，这样可以大大加大每个节点存储的key值数量，降低B+Tree的高度。</p>
<p>由于B+Tree的非叶子节点只存储键值信息，假设每个磁盘块能存储4个键值及指针信息，则变成B+Tree后其结构如下图所示：</p>
<figure data-type="image" tabindex="3"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d740deb6b609?w=1020&amp;h=379&amp;f=png&amp;s=39963" alt="B+Tree" loading="lazy"></figure>
<h2 id="建立索引的几大原则">建立索引的几大原则</h2>
<ol>
<li>
<p>最左前缀匹配原则，非常重要的原则，mysql会一直向右匹配直到遇到范围查询(&gt;、&lt;、between、like)就停止匹配，比如a = 1 and b = 2 and c &gt; 3 and d = 4 如果建立(a,b,c,d)顺序的索引，d是用不到索引的，如果建立(a,b,d,c)的索引则都可以用到，a,b,d的顺序可以任意调整。</p>
</li>
<li>
<p>=和in可以乱序，比如a = 1 and b = 2 and c = 3 建立(a,b,c)索引可以任意顺序，mysql的查询优化器会帮你优化成索引可以识别的形式。</p>
</li>
<li>
<p>尽量选择区分度高的列作为索引，区分度的公式是count(distinct col)/count(*)，表示字段不重复的比例，比例越大我们扫描的记录数越少，唯一键的区分度是1，而一些状态、性别字段可能在大数据面前区分度就是0，那可能有人会问，这个比例有什么经验值吗？使用场景不同，这个值也很难确定，一般需要join的字段我们都要求是0.1以上，即平均1条扫描10条记录。</p>
</li>
<li>
<p>索引列不能参与计算，保持列“干净”，比如from_unixtime(create_time) = ’2014-05-29’就不能使用到索引，原因很简单，b+树中存的都是数据表中的字段值，但进行检索时，需要把所有元素都应用函数才能比较，显然成本太大。所以语句应该写成create_time = unix_timestamp(’2014-05-29’)。</p>
</li>
<li>
<p>尽量的扩展索引，不要新建索引。比如表中已经有a的索引，现在要加(a,b)的索引，那么只需要修改原来的索引即可。</p>
</li>
</ol>
<h2 id="慢查询优化基本步骤">慢查询优化基本步骤</h2>
<ol>
<li>先运行看看是否真的很慢，注意设置SQL_NO_CACHE</li>
<li>where条件单表查，锁定最小返回记录表。这句话的意思是把查询语句的where都应用到表中返回的记录数最小的表开始查起，单表每个字段分别查询，看哪个字段的区分度最高</li>
<li>explain查看执行计划，是否与2预期一致（从锁定记录较少的表开始查询）</li>
<li>order by limit 形式的sql语句让排序的表优先查</li>
<li>了解业务方使用场景</li>
<li>加索引时参照建索引的几大原则</li>
<li>观察结果，不符合预期继续从0分析</li>
</ol>
<h3 id="explain详解">explain详解</h3>
<p>explain为mysql提供语句的执行计划信息。可以应用在select、delete、insert、update和place语句上。explain的执行计划，只是作为语句执行过程的一个参考，实际执行的过程不一定和计划完全一致，但是执行计划中透露出的信息却可以帮助选择更好的索引和写出更优化的查询语句。</p>
<p><strong>explain输出项</strong></p>
<table>
<thead>
<tr>
<th>Column</th>
<th style="text-align:center">JSON Name</th>
<th style="text-align:center">Meaning</th>
</tr>
</thead>
<tbody>
<tr>
<td>id</td>
<td style="text-align:center">select_id</td>
<td style="text-align:center">The SELECT identifier</td>
</tr>
<tr>
<td>select_type</td>
<td style="text-align:center">None</td>
<td style="text-align:center">The SELECT type</td>
</tr>
<tr>
<td>table</td>
<td style="text-align:center">table_name</td>
<td style="text-align:center">The table for the output row</td>
</tr>
<tr>
<td>partitions</td>
<td style="text-align:center">partitions</td>
<td style="text-align:center">The matching partitions</td>
</tr>
<tr>
<td>type</td>
<td style="text-align:center">access_type</td>
<td style="text-align:center">The join type</td>
</tr>
<tr>
<td>possible_keys</td>
<td style="text-align:center">possible_keys</td>
<td style="text-align:center">The possible indexes to choose</td>
</tr>
<tr>
<td>key</td>
<td style="text-align:center">key</td>
<td style="text-align:center">The index actually chosen</td>
</tr>
<tr>
<td>key_len</td>
<td style="text-align:center">key_length</td>
<td style="text-align:center">The length of the chosen key</td>
</tr>
<tr>
<td>ref</td>
<td style="text-align:center">ref</td>
<td style="text-align:center">The columns compared to the index</td>
</tr>
<tr>
<td>rows</td>
<td style="text-align:center">rows</td>
<td style="text-align:center">Estimate of rows to be examined</td>
</tr>
<tr>
<td>filtered</td>
<td style="text-align:center">filtered</td>
<td style="text-align:center">Percentage of rows filtered by table condition</td>
</tr>
<tr>
<td>Extra</td>
<td style="text-align:center">None</td>
<td style="text-align:center">Additional information</td>
</tr>
</tbody>
</table>
<h4 id="id">id</h4>
<p>id列的编号是 select 的序列号，有几个 select 就有几个id，并且id的顺序是按 select 出现的顺序增长的。</p>
<p>MySQL将 select 查询分为简单查询(SIMPLE)和复杂查询(PRIMARY)。复杂查询分为三类：简单子查询、派生表（from语句中的子查询）、union 查询。</p>
<blockquote>
<p><strong>id列越大执行优先级越高，id相同则从上往下执行，id为NULL最后执行</strong>。</p>
</blockquote>
<h4 id="select_type">select_type</h4>
<p>select_type 表示对应行是简单还是复杂的查询。</p>
<h4 id="table">table</h4>
<p>这一列表示 explain 的一行正在访问哪个表。</p>
<p>当 from 子句中有子查询时，table列是 <derivenN> 格式，表示当前查询依赖 id=N 的查询，于是先执行 id=N 的查询。</p>
<p>当有 union 时，UNION RESULT 的 table 列的值为&lt;union1, 2&gt;，1和2表示参与 union 的 select 行id。</p>
<h4 id="partitions">partitions</h4>
<h4 id="type">type</h4>
<p>这一列表示关联类型或访问类型，即MySQL决定如何查找表中的行，查找数据行记录的大概范围。</p>
<p>依次从最优到最差分别为：system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</p>
<ul>
<li><strong>NULL</strong>：<strong>mysql能够在优化阶段分解查询语句，在执行阶段用不着再访问表或索引。例如：在索引列中选取最小值，可以单独查找索引来完成，不需要在执行时访问表</strong>。</li>
<li>const, system：mysql能对查询的某部分进行优化并将其转化成一个常量（可以看show warnings 的结果）。用于 primary key 或 unique key 的所有列与常数比较时，所以表最多有一个匹配行，读取1次，速度比较快。system是const的特例，表里只有一条元组匹配时为system</li>
<li><strong>eq_ref</strong>：<strong>primary key 或 unique key 索引的所有部分被连接使用 ，最多只会返回一条符合条件的记录。这可能是在 const 之外最好的联接类型了，简单的 select 查询不会出现这种 type。</strong></li>
<li><strong>ref</strong>：<strong>相比 eq_ref，不使用唯一索引，而是使用普通索引或者唯一性索引的部分前缀，索引要和某个值相比较，可能会找到多个符合条件的行。</strong></li>
<li><strong>range</strong>：<strong>范围扫描通常出现在 in(), between , &gt; , &lt;, &gt;= 等操作中。使用一个索引来检索给定范围的行。</strong></li>
<li><strong>index</strong>：<strong>扫描全表索引，这通常比ALL快一些。（index是从索引中读取的，而all是从硬盘中读取）</strong></li>
<li><strong>ALL</strong>：<strong>即全表扫描，意味着mysql需要从头到尾去查找所需要的行。通常情况下这需要增加索引来进行优化了</strong></li>
</ul>
<h4 id="possible_keys">possible_keys</h4>
<p>这一列显示查询可能使用哪些索引来查找。</p>
<p>explain 时可能出现 possible_keys 有列，而 key 显示 NULL 的情况，这种情况是因为表中数据不多，mysql认为索引对此查询帮助不大，选择了全表查询。</p>
<p>如果该列是NULL，则没有相关的索引。在这种情况下，可以通过检查 where 子句看是否可以创造一个适当的索引来提高查询性能，然后用 explain 查看效果。</p>
<h4 id="key">key</h4>
<p>这一列显示mysql实际采用哪个索引来优化对该表的访问。</p>
<p>如果没有使用索引，则该列是 NULL。如果想强制mysql使用或忽视possible_keys列中的索引，在查询中使用 force index、ignore index。</p>
<h4 id="key_len">key_len</h4>
<p>这一列显示了mysql在索引里使用的字节数，通过这个值可以算出具体使用了索引中的哪些列。</p>
<h4 id="ref">ref</h4>
<p>这一列显示了在key列记录的索引中，表查找值所用到的列或常量，常见的有：const（常量），字段名（例：film. id）</p>
<h4 id="rows">rows</h4>
<p>这一列是mysql估计要读取并检测的行数，注意这个不是结果集里的行数。</p>
<h4 id="filtered">filtered</h4>
<h4 id="extra">Extra</h4>
<ul>
<li>
<p><strong>Using index</strong>：<strong>查询的列被索引覆盖，并且where筛选条件是索引的前导列(最左侧索引)，是性能高的表现。一般是使用了覆盖索引(索引包含了所有查询的字段)。对于innodb来说，如果是辅助索引性能会有不少提高</strong>。</p>
</li>
<li>
<p><strong>Using where</strong>：<strong>查询的列未被索引覆盖，where筛选条件非索引的前导列</strong>。</p>
</li>
<li>
<p><strong>Using where Using index</strong>：<strong>查询的列被索引覆盖，并且where筛选条件是索引列之一但不是索引的前导列，意味着无法直接通过索引查找来查询到符合条件的数据, Using index代表select用到了覆盖索引</strong>。</p>
</li>
<li>
<p><strong>NULL</strong>：<strong>查询的列未被索引覆盖，并且where筛选条件是索引的前导列，意味着用到了索引，但是部分字段未被索引覆盖，必须通过“回表”来实现，不是纯粹地用到了索引，也不是完全没用到索引</strong>。</p>
</li>
<li>
<p><strong>Using index condition</strong>：<strong>与Using where类似，查询的列不完全被索引覆盖，where条件中是一个前导列的范围；</strong></p>
</li>
<li>
<p><strong>Using temporary：mysql需要创建一张临时表来处理查询。出现这种情况一般是要进行优化的，首先是想到用索引来优化。</strong></p>
</li>
<li>
<p><strong>Using filesort：mysql 会对结果使用一个外部索引排序，而不是按索引次序从表里读取行。此时mysql会根据联接类型浏览所有符合条件的记录，并保存排序关键字和行指针，然后排序关键字并按顺序检索行信息。这种情况下一般也是要考虑使用索引来优化的。</strong></p>
</li>
</ul>
<h3 id="案列分析">案列分析</h3>
<h4 id="1-复杂语句写法">1. 复杂语句写法</h4>
<p>很多情况下，我们写SQL只是为了实现功能，这只是第一步，不同的语句书写方式对于效率往往有本质的差别，这要求我们对mysql的执行计划和索引原则有非常清楚的认识，请看下面的语句：</p>
<pre><code class="language-sql">select
    distinct cert.emp_id
from
    cm_log cl
    inner join (
        select
            emp.id as emp_id,
            emp_cert.id as cert_id
        from
            employee emp
            left join emp_certificate emp_cert on emp.id = emp_cert.emp_id
        where
            emp.is_deleted = 0
    ) cert on (
        cl.ref_table = 'Employee'
        and cl.ref_oid = cert.emp_id
    )
    or (
        cl.ref_table = 'EmpCertificate'
        and cl.ref_oid = cert.cert_id
    )
where
    cl.last_upd_date &gt;= '2013-11-07 15:03:00'
    and cl.last_upd_date &lt;= '2013-11-08 16:00:00';
</code></pre>
<ol>
<li>先运行一下，53条记录 1.87秒，又没有用聚合语句，比较慢</li>
</ol>
<pre><code class="language-bash">   53 rows in set (1.87 sec)
</code></pre>
<ol start="2">
<li>explain</li>
</ol>
<figure data-type="image" tabindex="4"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d73da79148e7?w=2622&amp;h=292&amp;f=png&amp;s=83974" alt="" loading="lazy"></figure>
<p>简述一下执行计划，首先mysql根据idx_last_upd_date索引扫描cm_log表获得379条记录；然后查表扫描了63727条记录，分为两部分，derived表示构造表，也就是不存在的表，可以简单理解成是一个语句形成的结果集，后面的数字表示语句的ID。derived2表示的是ID = 2的查询构造了虚拟表，并且返回了63727条记录。我们再来看看ID = 2的语句究竟做了写什么返回了这么大量的数据，首先全表扫描employee表13317条记录，然后根据索引emp_certificate_empid关联emp_certificate表，rows = 1表示，每个关联都只锁定了一条记录，效率比较高。获得后，再和cm_log的379条记录根据规则关联。从执行过程上可以看出返回了太多的数据，返回的数据绝大部分cm_log都用不到，因为cm_log只锁定了379条记录。</p>
<p>如何优化呢？可以看到我们在运行完后还是要和cm_log做join, 那么我们能不能之前和cm_log做join呢？仔细分析语句不难发现，其基本思想是如果cm_log的ref_table是EmpCertificate就关联emp_certificate表，如果ref_table是Employee就关联employee表，我们完全可以拆成两部分，并用union连接起来，注意这里用union，而不用union all是因为原语句有“distinct”来得到唯一的记录，而union恰好具备了这种功能。如果原语句中没有distinct不需要去重，我们就可以直接使用union all了，因为使用union需要去重的动作，会影响SQL性能。</p>
<p>优化过的语句如下：</p>
<pre><code class="language-sql">   select
       emp.id
   from
       cm_log cl
       inner join employee emp on cl.ref_table = 'Employee'
       and cl.ref_oid = emp.id
   where
       cl.last_upd_date &gt;= '2013-11-07 15:03:00'
       and cl.last_upd_date &lt;= '2013-11-08 16:00:00'
       and emp.is_deleted = 0
   union
   select
       emp.id
   from
       cm_log cl
       inner join emp_certificate ec on cl.ref_table = 'EmpCertificate'
       and cl.ref_oid = ec.id
       inner join employee emp on emp.id = ec.emp_id
   where
       cl.last_upd_date &gt;= '2013-11-07 15:03:00'
       and cl.last_upd_date &lt;= '2013-11-08 16:00:00'
       and emp.is_deleted = 0
</code></pre>
<ol start="4">
<li>
<p>不需要了解业务场景，只需要改造的语句和改造之前的语句保持结果一致</p>
</li>
<li>
<p>现有索引可以满足，不需要建索引</p>
</li>
<li>
<p>用改造后的语句实验一下，只需要10ms 降低了近200倍！   <img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d73da7f5d7c8?w=2250&amp;h=376&amp;f=png&amp;s=110266" alt="" loading="lazy"></p>
</li>
</ol>
<h4 id="2-明确应用场景">2. 明确应用场景</h4>
<p>举这个例子的目的在于颠覆我们对列的区分度的认知，一般上我们认为区分度越高的列，越容易锁定更少的记录，但在一些特殊的情况下，这种理论是有局限性的。</p>
<pre><code class="language-sql">select
  *
from
  stage_poi sp
where
  sp.accurate_result = 1
  and (
      sp.sync_status = 0
      or sp.sync_status = 2
      or sp.sync_status = 4
  );
</code></pre>
<ol>
<li>先看看运行多长时间,951条数据6.22秒，真的很慢。</li>
</ol>
<pre><code class="language-bash">951 rows in set (6.22 sec)
</code></pre>
<ol start="2">
<li>先explain，rows达到了361万，type = ALL表明是全表扫描。</li>
</ol>
<figure data-type="image" tabindex="5"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d73dac521c54?w=1454&amp;h=196&amp;f=png&amp;s=29322" alt="" loading="lazy"></figure>
<ol start="3">
<li>
<p>所有字段都应用查询返回记录数，因为是单表查询 0已经做过了951条。</p>
</li>
<li>
<p>让explain的rows 尽量逼近951。</p>
<p>看一下accurate_result = 1的记录数：</p>
</li>
</ol>
<pre><code class="language-sql">select count(*),accurate_result from stage_poi  group by accurate_result;
+----------+-----------------+
| count(*) | accurate_result |
+----------+-----------------+
|     1023 |              -1 |
|  2114655 |               0 |
|   972815 |               1 |
+----------+-----------------+
</code></pre>
<p>我们看到accurate_result这个字段的区分度非常低，整个表只有-1, 0, 1三个值，加上索引也无法锁定特别少量的数据。</p>
<p>再看一下sync_status字段的情况：</p>
<pre><code class="language-sql">select count(*),sync_status from stage_poi  group by sync_status;
+----------+-------------+
| count(*) | sync_status |
+----------+-------------+
|     3080 |           0 |
|  3085413 |           3 |
+----------+-------------+
</code></pre>
<p>同样的区分度也很低，根据理论，也不适合建立索引。</p>
<p>问题分析到这，好像得出了这个表无法优化的结论，两个列的区分度都很低，即便加上索引也只能适应这种情况，很难做普遍性的优化，比如当sync_status 0、3分布的很平均，那么锁定记录也是百万级别的。</p>
<p>找业务方去沟通，看看使用场景。业务方是这么来使用这个SQL语句的，每隔五分钟会扫描符合条件的数据，处理完成后把sync_status这个字段变成1, 五分钟符合条件的记录数并不会太多，1000个左右。了解了业务方的使用场景后，优化这个SQL就变得简单了，因为业务方保证了数据的不平衡，如果加上索引可以过滤掉绝大部分不需要的数据。</p>
<ol start="5">
<li>根据建立索引规则，使用如下语句建立索引</li>
</ol>
<pre><code class="language-sql">alter table stage_poi add index idx_acc_status(accurate_result,sync_status);
</code></pre>
<ol start="6">
<li>观察预期结果,发现只需要200ms，快了30多倍。</li>
</ol>
<pre><code class="language-sql">952 rows in set (0.20 sec)
</code></pre>
<p>我们再来回顾一下分析问题的过程，单表查询相对来说比较好优化，大部分时候只需要把where条件里面的字段依照规则加上索引就好，如果只是这种“无脑”优化的话，显然一些区分度非常低的列，不应该加索引的列也会被加上索引，这样会对插入、更新性能造成严重的影响，同时也有可能影响其它的查询语句。所以我们第4步调差SQL的使用场景非常关键，我们只有知道这个业务场景，才能更好地辅助我们更好的分析和优化查询语句。</p>
<h4 id="3-无法优化的语句">3. 无法优化的语句</h4>
<pre><code class="language-sql">select
   c.id,
   c.name,
   c.position,
   c.sex,
   c.phone,
   c.office_phone,
   c.feature_info,
   c.birthday,
   c.creator_id,
   c.is_keyperson,
   c.giveup_reason,
   c.status,
   c.data_source,
   from_unixtime(c.created_time) as created_time,
   from_unixtime(c.last_modified) as last_modified,
   c.last_modified_user_id
from
   contact c
   inner join contact_branch cb on c.id = cb.contact_id
   inner join branch_user bu on cb.branch_id = bu.branch_id
   and bu.status in (1, 2)
   inner join org_emp_info oei on oei.data_id = bu.user_id
   and oei.node_left &gt;= 2875
   and oei.node_right &lt;= 10802
   and oei.org_category = - 1
order by
   c.created_time desc
limit
   0, 10;
</code></pre>
<p>还是几个步骤。</p>
<ol>
<li>先看语句运行多长时间，10条记录用了13秒，已经不可忍受。</li>
</ol>
<pre><code class="language-bash">10 rows in set (13.06 sec)
</code></pre>
<ol start="2">
<li>
<p>explain</p>
<figure data-type="image" tabindex="6"><img src="https://user-gold-cdn.xitu.io/2020/4/21/1719d73dd6133451?w=2790&amp;h=270&amp;f=png&amp;s=91077" alt="" loading="lazy"></figure>
<p>从执行计划上看，mysql先查org_emp_info表扫描8849记录，再用索引idx_userid_status关联branch_user表，再用索引idx_branch_id关联contact_branch表，最后主键关联contact表。</p>
<p>rows返回的都非常少，看不到有什么异常情况。我们在看一下语句，发现后面有order by + limit组合，会不会是排序量太大搞的？于是我们简化SQL，去掉后面的order by 和 limit，看看到底用了多少记录来排序。</p>
</li>
</ol>
<pre><code class="language-sql">select
 count(*)
from
  contact c  
inner join
  contact_branch cb 
     on  c.id = cb.contact_id  
inner join
  branch_user bu 
     on  cb.branch_id = bu.branch_id 
     and bu.status in (
        1,
     2)  
  inner join
     org_emp_info oei 
        on  oei.data_id = bu.user_id 
        and oei.node_left &gt;= 2875 
        and oei.node_right &lt;= 10802 
        and oei.org_category = - 1  

+----------+
| count(*) |
+----------+
|   778878 |
+----------+
1 row in set (5.19 sec)
</code></pre>
<p>发现排序之前居然锁定了778878条记录，如果针对70万的结果集排序，将是灾难性的，怪不得这么慢，那我们能不能换个思路，先根据contact的created_time排序，再来join会不会比较快呢？</p>
<p>于是改造成下面的语句，也可以用straight_join来优化：</p>
<pre><code class="language-sql">select
   c.id,
   c.name,
   c.position,
   c.sex,
   c.phone,
   c.office_phone,
   c.feature_info,
   c.birthday,
   c.creator_id,
   c.is_keyperson,
   c.giveup_reason,
   c.status,
   c.data_source,
   from_unixtime(c.created_time) as created_time,
   from_unixtime(c.last_modified) as last_modified,
   c.last_modified_user_id
from
   contact c
where
   exists (
       select
           1
       from
           contact_branch cb
           inner join branch_user bu on cb.branch_id = bu.branch_id
           and bu.status in (1, 2)
           inner join org_emp_info oei on oei.data_id = bu.user_id
           and oei.node_left &gt;= 2875
           and oei.node_right &lt;= 10802
           and oei.org_category = - 1
       where
           c.id = cb.contact_id
   )
order by
   c.created_time desc
limit
   0, 10;
</code></pre>
<p>验证一下效果 预计在1ms内，提升了13000多倍！</p>
<pre><code class="language-bash">10 rows in set (0.00 sec)
</code></pre>
<p>本以为至此大工告成，但我们在前面的分析中漏了一个细节，先排序再join和先join再排序理论上开销是一样的，为何提升这么多是因为有一个limit！大致执行过程是：mysql先按索引排序得到前10条记录，然后再去join过滤，当发现不够10条的时候，再次去10条，再次join，这显然在内层join过滤的数据非常多的时候，将是灾难的，极端情况，内层一条数据都找不到，mysql还傻乎乎的每次取10条，几乎遍历了这个数据表！</p>
<p>​		用不同参数的SQL试验下：</p>
<pre><code class="language-sql">select
  sql_no_cache c.id,
  c.name,
  c.position,
  c.sex,
  c.phone,
  c.office_phone,
  c.feature_info,
  c.birthday,
  c.creator_id,
  c.is_keyperson,
  c.giveup_reason,
  c.status,
  c.data_source,
  from_unixtime(c.created_time) as created_time,
  from_unixtime(c.last_modified) as last_modified,
  c.last_modified_user_id
from
  contact c
where
  exists (
      select
          1
      from
          contact_branch cb
          inner join branch_user bu on cb.branch_id = bu.branch_id
          and bu.status in (1, 2)
          inner join org_emp_info oei on oei.data_id = bu.user_id
          and oei.node_left &gt;= 2875
          and oei.node_right &lt;= 2875
          and oei.org_category = - 1
      where
          c.id = cb.contact_id
  )
order by
  c.created_time desc
limit
  0, 10;

Empty set (2 min 18.99 sec)
</code></pre>
<p>2 min 18. 99 sec！比之前的情况还糟糕很多。由于mysql的nested loop机制，遇到这种情况，基本是无法优化的。这条语句最终也只能交给应用系统去优化自己的逻辑了。</p>
<p>通过这个例子我们可以看到，并不是所有语句都能优化，而往往我们优化时，由于SQL用例回归时落掉一些极端情况，会造成比原来还严重的后果。所以，第一：不要指望所有语句都能通过SQL优化，第二：不要过于自信，只针对具体case来优化，而忽略了更复杂的情况。</p>
<p>慢查询的案例就分析到这儿，以上只是一些比较典型的案例。我们在优化过程中遇到过超过1000行，涉及到16个表join的“垃圾SQL”，也遇到过线上线下数据库差异导致应用直接被慢查询拖死，也遇到过varchar等值比较没有写单引号，还遇到过笛卡尔积查询直接把从库搞死。再多的案例其实也只是一些经验的积累，如果我们熟悉查询优化器、索引的内部原理，那么分析这些案例就变得特别简单了。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java基础：字符串String]]></title>
        <id>https://blockmao.github.io/gb8Y95DIJ/</id>
        <link href="https://blockmao.github.io/gb8Y95DIJ/">
        </link>
        <updated>2020-04-11T07:51:42.000Z</updated>
        <summary type="html"><![CDATA[<ul>
<li>不可变String</li>
<li>重载 “+” 与StringBuilder</li>
<li>格式化输出</li>
<li>正则表达式</li>
</ul>
]]></summary>
        <content type="html"><![CDATA[<ul>
<li>不可变String</li>
<li>重载 “+” 与StringBuilder</li>
<li>格式化输出</li>
<li>正则表达式</li>
</ul>
<!-- more -->
<blockquote>
<p><strong>可以证明，字符串操作是计算机程序设计中最常见的行为。</strong></p>
</blockquote>
<h2 id="不可变string">不可变String</h2>
<p>String对象是不可变的。查看JDK文档你就会发现，String类中每一个看起来会修改String值的方法，实际上都是创建了一个全新的String对象，以包含修改后的字符串内容，而最初的String对象则丝毫未变。</p>
<pre><code class="language-java">public class Immutable {

    public static String upcase(String s){
        return  s.toUpperCase();
    }

    public static void main(String[] args) {
        String q = &quot;howdy&quot;;
        System.out.println(q);
        String qq = upcase(q);
        System.out.println(qq);
        System.out.println(q);
    }
}
</code></pre>
<h2 id="重载-与stringbuilder">重载 “+” 与StringBuilder</h2>
<p>不可变性会带来一定的效率问题。用于String的“+”与“+=”是Java中仅有的两个重载过的操作符，而Java并不允许程序重载任何操作符。</p>
<p>操作符 “+” 可以用来连接String：</p>
<pre><code class="language-java">public class Concatenation {

    public static void main(String[] args) {
        String mango = &quot;mango&quot;;
        String s = &quot;abc&quot; + mango;
        System.out.println(s);
    }
}
</code></pre>
<p>想看看以上代码到底是如何工作的吗，可以用JDK自带的工具javap来反编译以上代码，可以得到以下的字节码：</p>
<figure data-type="image" tabindex="1"><img src="https://upload-images.jianshu.io/upload_images/2889214-8ab86b0285d9dcbf.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240#width=" alt="" loading="lazy"></figure>
<p>编译器自动引入了java.lang.StringBuilder类（因为它更高效），通过调用append方法将字符串连接起来，最后调用toString方法生成最终结果。</p>
<h3 id="避免循环体内使用">避免循环体内使用 &quot;+=&quot;</h3>
<pre><code class="language-java">public String implicit(String[] fields) {
    String result = &quot;&quot;;
    for (int i = 0; i &lt; fields.length; i++) {
        result += fields[i];
    }
    return result;
}
</code></pre>
<p>通过反编译得如下字节码：</p>
<figure data-type="image" tabindex="2"><img src="https://upload-images.jianshu.io/upload_images/2889214-8cd5414821484264.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240#width=" alt="" loading="lazy"></figure>
<p>StringBuilder是在循环体内构造的，这意味着每经过循环一次，就会创建一个新的StringBuilder对象。</p>
<h3 id="无意识的递归">无意识的递归</h3>
<pre><code class="language-java">public class InfiniteRecursion {

    @Override
    public String toString() {
        // 打印InfiniteRecursion对象的内存地址
        return &quot; InfiniteRecursion adress: &quot; + this + &quot;\n&quot;;
    }

    public static void main(String[] args) {
        InfiniteRecursion infiniteRecursion  = new InfiniteRecursion();
        System.out.println(infiniteRecursion);
    }
}
</code></pre>
<p>运行以上程序出现如下结果：</p>
<figure data-type="image" tabindex="3"><img src="https://upload-images.jianshu.io/upload_images/2889214-b41c42bd105ac064.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240#width=" alt="" loading="lazy"></figure>
<p><code>&quot; InfiniteRecursion adress: &quot; + this</code>这里发生了自动类型转换，由InfiniteRecursion类型转换成String类型。因为编译器发现String对象后面跟着一个 “+”，而后面的对象不是String，编译器调用this.toString()方法进行类型转换，因此发生了递归调用。</p>
<p>如果你真的想要打印出对象的内存地址，应该调用Object.toString()方法。所以不该使用this，而是应该调用super.toString()方法。</p>
<h2 id="格式化输出">格式化输出</h2>
<p>Java SE5推出了C语言中printf()风格的格式化输出这一功能，不需要使用重载的 “+”操作符来连接引用号内的字符串或者字符串常量，而是使用特殊的占位符来表示数据将来的位置。</p>
<pre><code class="language-java">public static void main(String[] args) {
    int x = 5;
    double y = 5.332;

    // the old way
    System.out.println(&quot;Row 1：[&quot; + x + &quot; &quot; + y + &quot;]&quot;);

    // the new way
    System.out.printf(&quot;Row 1：[%d %f]\n&quot;, x, y);
    
    // or
    System.out.format(&quot;Row 1：[%d %f]\n&quot;, x, y);
}
</code></pre>
<p>运行以上程序，首先将x的值插入到%d的位置，然后将y的值插入%f的位置。这些占位符被称为<strong>格式修饰符</strong>，它们不但说明了将插入什么类型的变量，以及如何对其格式化。</p>
<h3 id="formatter类">Formatter类</h3>
<p>在Java中，所有新的格式化功能都由java.util.Formatter类处理。可以将Formatter类看作一个翻译器，它将你的格式化字符串与数据翻译成需要的结果。</p>
<pre><code class="language-java">Formatter formatter = new Formatter(System.out);
formatter.format(&quot;Row 1：[%d %f]\n&quot;, x, y);
</code></pre>
<h3 id="stringformat">String.format()</h3>
<p>String.format()是一个static方法，它接受与Formatter.format()方法一样的参数，但返回一个String对象。</p>
<h3 id="格式化说明符">格式化说明符</h3>
<p>在插入数据时，如果想要控制空格与对齐，你需要更精细复杂的格式修饰符。以下是其抽象的语法：</p>
<pre><code>%[argument_index$][flags][width][.precision]conversion
</code></pre>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>argument_index</td>
<td>需要将参数列表中第几个参数进行格式化</td>
</tr>
<tr>
<td>flags</td>
<td>一些特殊的格式，比如‘-’代表向左对齐</td>
</tr>
<tr>
<td>width</td>
<td>输出的最小的宽度，适用于所有的参数类型</td>
</tr>
<tr>
<td>[.precision]</td>
<td>参数为String，则表示打印String输出字符的最大数量；参数为float，则表示小数点最大位数。不使用于int</td>
</tr>
<tr>
<td>conversion</td>
<td>接受的参数类型，如s代表后面接String类型的参数；d代表接int型的参数</td>
</tr>
</tbody>
</table>
<p><a href="http://blog.csdn.net/XIAXIA__/article/details/41720485">参数详细说明</a></p>
<h2 id="正则表达式">正则表达式</h2>
<p>正则表达式是一种强大而灵活的文本处理工具。使用正则表达式，我们能够以编程的方式，构造复杂的文本模式，并对输入的字符串进行搜索。一旦找到了匹配这些模式的部分，你就能够随心所欲地对它们进行处理。</p>
<blockquote>
<p><strong>Java语言与其他语言相比对反斜杠<code>\</code>有不同的处理</strong>：</p>
<p>在其他语言中，<code>\\</code>表示“我想要在正则表达式中插入一个普通的（字面上的）反斜杠，请不要给它做任何特殊的意义。” 而在Java中，<code>\\</code>的意思是“我要插入一个正则表达式的反斜杠，所以其后的字符具有特殊的意义。”</p>
<p>例如，如果你想表示一位数字，那么正则表达式应该是<code>\\d</code>。如果你想插入一个普通的反斜杠，则应该这样<code>\\\\</code>。不过换行和制表符之类的东西只需使用单斜杠线：<code>\n\t</code>。</p>
</blockquote>
<h3 id="string类支持正则表达式的方法">String类支持正则表达式的方法</h3>
<pre><code class="language-java">public String[] split(String regex)

public String[] split(String regex, int limit) 

public String replaceFirst(String regex, String replacement)

public String replaceAll(String regex, String replacement) 

public boolean matches(String regex)
</code></pre>
<h3 id="pattern和matcher">Pattern和Matcher</h3>
<p>一般来说，比起功能有限的String类，我们更愿意构造功能强大的正则表达式对象。通过<code>Pattern.complie()</code>方法来编译你的正则表达式即可。它会根据你的String类型的正则表达式生成一个Pattern对象。接下来，把你想要检索的字符串传入Pattern对象的matcher()方法，matcher()方法会生成一个Matcher对象，它有很多功能可用。</p>
<pre><code class="language-java">public class RegexExpression {

    public static void main(String[] args) {
        String phone = &quot;18926119073&quot;;
        Pattern pattern = Pattern.compile(&quot;1([358][0-9]|4[579]|66
                |7[0135678]|9[89])[0-9]{8}&quot;);
        Matcher matcher = pattern.matcher(phone);
        System.out.println(matcher.matches());
    }
}
</code></pre>
<h3 id="组">组</h3>
<p>组是用括号划分的正则表达式，可以根据组的编号来引用某个组。组号为0表示整个表达式，组号1表示被第一对括号括起的组，依次类推。因此，在下面这个表达式：</p>
<pre><code>A(B(C))D
</code></pre>
<p>中有三个组：组0是ABCD，组1是BC，组2是C。</p>
<p>Mather提供了很多有用的方法具体使用查看API,使用Mather的替换方法可以实现隐藏手机号中间数字、隐藏用户名等。</p>
<pre><code class="language-java">String phone = &quot;18926119073&quot;;
Pattern pattern = Pattern.compile(&quot;(\\d{3})\\d{4}(\\d{4})&quot;);
Matcher matcher = pattern.matcher(phone);
phone = matcher.replaceAll(&quot;$1****$2&quot;);
System.out.println(phone);
</code></pre>
<h3 id="用正则表达式扫描">用正则表达式扫描</h3>
<p>Java SE5新增类Scanner类，它可以大大减轻扫描输入的工作。</p>
<pre><code class="language-java">public class ScannerRead {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(&quot;Sir Robin of Camelot
        \n22 1.61803&quot;));
        System.out.println(&quot;What is your name?&quot;);
        String name = scanner.nextLine();
        System.out.println(name);
        System.out.println(&quot;(input: &lt;age&gt; &lt;double&gt;)&quot;);
        System.out.println(scanner.nextInt()+&quot; &quot;+ scanner.nextDouble());
    }
}
</code></pre>
<blockquote>
<p>Scanner的构造期可以接受任何类型的输入对象，包括File对象、InputStream、String或者Readable对象。</p>
<p>Scanner所有的输入、分词以及翻译的操作都隐藏在不同类型的next方法中，普通的next()方法返回下一个String，所有的基本类型（除char之外）都有对应的next方法，包括BigDecimal和BigInteger。所有的next方法，只有在找到一个完整的分词之后才会返回，hasNext方法用以判断下一个输入分词是否所需的类型。</p>
</blockquote>
<p>在默认的情况下，Scanner根据空白符对输入进行分词，但是你可以用正则表达式指定自己所需的定界符：</p>
<pre><code class="language-java">public class ScannerDelimiter {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(&quot;12,42,78,99&quot;);
        scanner.useDelimiter(&quot;,&quot;);
        while (scanner.hasNextInt()){
            System.out.println(scanner.nextInt());
        }
    }
}
</code></pre>
<p>除了能够扫描基本类型之外，你还可以使用自定义的正则表达式进行扫描。如下所示：</p>
<pre><code class="language-java">public class ThreatAnalyzer {

    static String threatData =
            &quot;58.27.82.161@02/10/2005\n&quot; +
            &quot;124.45.82.161@02/10/2005\n&quot; +
            &quot;58.27.82.161@02/10/2005\n&quot; +
            &quot;72.27.82.161@02/10/2005\n&quot; +
             &quot;[Next log section with different data format]&quot;;

    public static void main(String[] args) {
        Scanner scanner = new Scanner(threatData);
        String pattern = &quot;(\\d+[.]\\d+[.]\\d+[.]\\d+)@(\\d{2}/\\d{2}/\\d{4})&quot;;
        while (scanner.hasNext(pattern)) {
            scanner.next(pattern);
            MatchResult match = scanner.match();
            String ip = match.group(1);
            String date = match.group(2);
            System.out.format(&quot;Threat on %s from %s\n&quot;, date, ip);
        }
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java基础：SPI详解]]></title>
        <id>https://blockmao.github.io/0wSmAUG7K/</id>
        <link href="https://blockmao.github.io/0wSmAUG7K/">
        </link>
        <updated>2020-04-11T07:35:32.000Z</updated>
        <summary type="html"><![CDATA[<p>SPI是Service Provider Interface的缩写，可以使用它扩展框架和更换的组件。JDK提供了java.util.ServiceLoader工具类，在使用某个服务接口时，它可以帮助我们查找该服务接口的实现类，加载和初始化，前提条件是基于它的约定。</p>
]]></summary>
        <content type="html"><![CDATA[<p>SPI是Service Provider Interface的缩写，可以使用它扩展框架和更换的组件。JDK提供了java.util.ServiceLoader工具类，在使用某个服务接口时，它可以帮助我们查找该服务接口的实现类，加载和初始化，前提条件是基于它的约定。</p>
<!-- more -->
<p>大多数开发人员可能不熟悉，却经常使用它。举个例子，获取MySQL数据库连接，代码如下：</p>
<pre><code class="language-java">public class MySQLConnect {

    private final static String url =&quot;jdbc:mysql://localhost:3306/test&quot;;

    private final static String username = &quot;root&quot;;

    private final static String password = &quot;root&quot;;

    public static Connection getConnection() throws SQLException {
       // Class.forName(&quot;com.mysql.jdbc.Driver&quot;);
       return DriverManager.getConnection(url,username,password);
    }

    public static void main(String[] args) throws SQLException {
        System.out.println(getConnection());
    }
}
</code></pre>
<p>上述代码是可以运行成功的。接下来我们就分析下DriverManager.getConnection(url,username,password)的过程，探究其如何获取到数据库连接Connection？</p>
<p>1、首先分析<code>loadInitialDrivers</code>方法，其源码如下：</p>
<pre><code class="language-java">private static void loadInitialDrivers() {
        
    /**
     * 获取ServiceLoader实例，loadedDrivers = 
     * new ServiceLoader(Driver.class,Thread.currentThread().getContextClassLoader())
     */
    ServiceLoader&lt;Driver&gt; loadedDrivers = ServiceLoader.load(Driver.class);
    /**
      * 获取serviceLoader实例的迭代器,driversIterator = new LazyIterator(service, loader)
      */
    Iterator&lt;Driver&gt; driversIterator = loadedDrivers.iterator();

    /**
      * 应用程序加载器（Thread.currentThread().getContextClassLoader()）
      * 加载classpath下META-INF/services/java.sql.Driver文件，
      * 解析java.sql.Driver文件的内容将其存储在LazyIterator.pending实例变量中。
      */
    while(driversIterator.hasNext()) {
        /**
        * 通过反射实例化java.sql.Driver文件中的类（com.mysql.jdbc.Driver，
            com.mysql.fabric.jdbc.FabricMySQLDriver）
        */
        driversIterator.next();
    }
}
</code></pre>
<blockquote>
<p>约定：当服务的提供者，提供了服务接口（java.sql.Driver）的一种实现之后，在jar包的META-INF/services/目录里同时创建一个以服务接口命名的文件。该文件里就是实现该服务接口的具体实现类。而当外部程序装配这个模块的时候，就能通过该jar包META-INF/services/里的配置文件找到具体的实现类名，并装载实例化，完成模块的注入。</p>
</blockquote>
<p>2、com.mysql.jdbc.Driver类的初始化</p>
<pre><code class="language-java">static {
    try {
        // registeredDrivers存储Dirver实例
        java.sql.DriverManager.registerDriver(new Driver());
    } catch (SQLException E) {
        throw new RuntimeException(&quot;Can't register driver!&quot;);
    }
}
</code></pre>
<p>3、获取数据库连接</p>
<pre><code class="language-java">for(DriverInfo aDriver : registeredDrivers) {

    if(isDriverAllowed(aDriver.driver, callerCL)) {
        try {
            // 获取Connection
            Connection con = aDriver.driver.connect(url, info);
            if (con != null) {
                return (con);
            }
        } catch (SQLException ex) {
            if (reason == null) {
                reason = ex;
            }
        }

    } else {
        println(&quot;skipping: &quot; + aDriver.getClass().getName());
    }

}
</code></pre>
<br/>
<h2 id="使用示例">使用示例</h2>
<p>1、目录结构</p>
<figure data-type="image" tabindex="1"><img src="https://blockmao.github.io/post-images/1586590964854.png" alt="" loading="lazy"></figure>
<p>2、示例代码</p>
<pre><code class="language-java">
public interface HelloService {

    String sayHello();

}

public class CHelloService implements HelloService {

    @Override
    public String sayHello() {
        return &quot;Welcome to C world&quot;;
    }

}

public class JavaHelloService implements HelloService {

    @Override
    public String sayHello() {
        return &quot;Welcome to Java world&quot;;
    }

}
</code></pre>
<p>3、com.codersm.study.jdk.spi.HelloService文件内容</p>
<pre><code class="language-bash">com.codersm.study.jdk.spi.impl.CHelloService
com.codersm.study.jdk.spi.impl.JavaHelloService
</code></pre>
<p>4、测试</p>
<pre><code class="language-java">
@Test
public void testSpi() {

    ServiceLoader&lt;HelloService&gt; loaders = ServiceLoader.load(HelloService.class);
    for (HelloService loader : loaders) {
        System.out.println(loader.sayHello());
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CentOS 7 下 yum 安装 Docker CE]]></title>
        <id>https://blockmao.github.io/A0vFvyiTq/</id>
        <link href="https://blockmao.github.io/A0vFvyiTq/">
        </link>
        <updated>2020-04-11T07:20:03.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p><strong>原文地址</strong>：<a href="https://qizhanming.com/blog/2019/01/25/how-to-install-docker-ce-on-centos-7">CentOS 7 下 yum 安装 Docker CE</a></p>
</blockquote>
<p><a name="d5i3v"></a></p>
<h2 id="前言">前言</h2>
<p><br />Docker 使用越来越多，安装也很简单，本次记录一下基本的步骤。<br /><br>
<br />Docker 目前支持 CentOS 7 及以后的版本，内核要求至少为 3.10。<br /><br>
<br />Docker 官网有安装步骤，本文只是记录一下，您也可以参考 <a href="https://docs.docker.com/install/linux/docker-ce/centos/">Get Docker CE for CentOS</a><br /></p>
<p><a name="6JkKE"></a></p>
<h3 id="环境说明">环境说明</h3>
<p><br />CentOS 7（Minimal Install）<br /></p>
<pre><code class="language-bash">$ cat /etc/redhat-release 
CentOS Linux release 7.6.1810 (Core)
</code></pre>
<p><a name="y8QOP"></a></p>
<h2 id="准备工作">准备工作</h2>
<p><a name="ZK0VP"></a></p>
<h3 id="操作系统要求">操作系统要求</h3>
<p>CentOS 7 以后都可以安装 Docker 了，也可以确认一下。<br /></p>
<pre><code class="language-bash">$ uname -a
Linux localhost.localdomain 3.10.0-957.1.3.el7.x86_64 #1 SMP Thu Nov 29 14:49:43 UTC 2018 x86_64 x86_64 x86_64 GNU/Linux
</code></pre>
<p><br />Docker 需要用到 <code>centos-extra</code> 这个源，如果您关闭了，需要重启启用，可以参考 <a href="https://wiki.centos.org/AdditionalResources/Repositories">Available Repositories for CentOS</a>。<br /></p>
<p><a name="ylc8Y"></a></p>
<h3 id="卸载旧版本">卸载旧版本</h3>
<p><br />旧版本的 Docker 被叫做 <code>docker</code> 或 <code>docker-engine</code>，如果您安装了旧版本的 Docker ，您需要卸载掉它。<br /></p>
<pre><code class="language-bash">$ sudo yum remove docker \
                  docker-client \
                  docker-client-latest \
                  docker-common \
                  docker-latest \
                  docker-latest-logrotate \
                  docker-logrotate \
                  docker-engine
</code></pre>
<p><br />旧版本的内容在 <code>/var/lib/docker</code> 下，目录中的镜像(images), 容器(containers), 存储卷(volumes), 和 网络配置（networks）都可以保留。<br />Docker CE 包，目前的包名为 <code>docker-ce</code>。<br>
<a name="3vgIu"></a></p>
<h2 id="安装">安装</h2>
<p><a name="dWg0t"></a></p>
<h3 id="安装准备">安装准备</h3>
<p>为了方便添加软件源，支持 devicemapper 存储类型，安装如下软件包<br /></p>
<pre><code class="language-bash">$ sudo yum update
$ sudo yum install -y yum-utils \
  device-mapper-persistent-data \
  lvm2
</code></pre>
<p><a name="VD3WR"></a></p>
<h3 id="添加-yum-软件源">添加 yum 软件源</h3>
<p><br />添加 Docker 稳定版本的 yum 软件源<br /></p>
<pre><code class="language-bash">$ sudo yum-config-manager \
    --add-repo \
    https://download.docker.com/linux/centos/docker-ce.repo
</code></pre>
<p><a name="rPtX0"></a></p>
<h3 id="安装-docker">安装 Docker</h3>
<p><br />更新一下 yum 软件源的缓存，并安装 Docker。<br /></p>
<pre><code class="language-bash">$ sudo yum update
$ sudo yum install docker-ce
</code></pre>
<p><br />如果弹出 GPG key 的接收提示，请确认是否为 <code>060a 61c5 1b55 8a7f 742b 77aa c52f eb6b 621e 9f35</code>，如果是，可以接受并继续安装。<br /><br>
<br />至此，Docker 已经安装完成了，Docker 服务是没有启动的，操作系统里的 docker 组被创建，但是没有用户在这个组里。<br /></p>
<blockquote>
<p><strong><code>注意</code></strong><br>
默认的 docker 组是没有用户的（也就是说需要使用 sudo 才能使用 docker 命令）。<br>
您可以将用户添加到 docker 组中（此用户就可以直接使用 docker 命令了）。</p>
</blockquote>
<p><br />加入 docker 用户组命令<br /></p>
<pre><code class="language-bash">$ sudo usermod -aG docker USER_NAME
</code></pre>
<p><br />用户更新组信息后，重新登录系统即可生效。<br>
<a name="AIOUx"></a></p>
<h3 id="安装指定版本">安装指定版本</h3>
<p>如果想安装指定版本的 Docker，可以查看一下版本并安装。<br /></p>
<pre><code class="language-bash">$ yum list docker-ce --showduplicates | sort -r
docker-ce.x86_64  3:18.09.1-3.el7                     docker-ce-stable
docker-ce.x86_64  3:18.09.0-3.el7                     docker-ce-stable
docker-ce.x86_64  18.06.1.ce-3.el7                    docker-ce-stable
docker-ce.x86_64  18.06.0.ce-3.el7                    docker-ce-stable
</code></pre>
<p><br />可以指定版本安装,版本号可以忽略 <code>:</code> 和 <code>el7</code>，如 <code>docker-ce-18.09.1</code><br /></p>
<pre><code>$ sudo yum install docker-ce-&lt;VERSION STRING&gt;
</code></pre>
<p><br />至此，指定版本的 Docker 也安装完成，同样，操作系统内 docker 服务没有启动，只创建了 docker 组，而且组里没有用户。<br>
<a name="eSjoh"></a></p>
<h3 id="启动-docker">启动 Docker</h3>
<p><br />如果想添加到开机启动<br /></p>
<pre><code class="language-bash">$ sudo systemctl enable docker
</code></pre>
<p><br />启动 docker 服务<br /></p>
<pre><code class="language-bash">$ sudo systemctl start docker
</code></pre>
<p><a name="a6v4n"></a></p>
<h3 id="验证安装">验证安装</h3>
<p>验证 Docker CE 安装是否正确，可以运行 <code>hello-world</code> 镜像<br /></p>
<pre><code class="language-bash">$ sudo docker run hello-world
</code></pre>
<p><a name="VTVH7"></a></p>
<h2 id="docker-compose安装">Docker Compose安装</h2>
<p><br />官网安装步骤地址：<a href="https://docs.docker.com/compose/install/#install-compose">https://docs.docker.com/compose/install/#install-compose</a></p>
<p>1、Run this command to download the latest version of Docker Compose<br /></p>
<pre><code class="language-bash">sudo curl -L &quot;https://github.com/docker/compose/releases/download/1.25.4/docker-compose-$(uname -s)-$(uname -m)&quot; -o /usr/local/bin/docker-compose
</code></pre>
<p><br />2、Apply executable permissions to the binary:<br /></p>
<pre><code class="language-bash">sudo chmod +x /usr/local/bin/docker-compose
</code></pre>
<p><br />3、Optionally, install command completion for the bash and zsh shell.<br /><br>
<br />4、Test the installation<br /></p>
<pre><code class="language-bash">docker-compose --version
</code></pre>
<p><a name="zt1v8"></a></p>
<h2 id="更新和卸载-docker">更新和卸载 Docker</h2>
<p><br />使用 yum 管理，更新和卸载都很方便。<br /></p>
<p><a name="yNB8N"></a></p>
<h3 id="更新-docker-ce">更新 Docker CE</h3>
<pre><code class="language-bash">$ sudo yum update docker-ce
</code></pre>
<p><a name="SBKxu"></a></p>
<h3 id="卸载-docker-ce">卸载 Docker CE</h3>
<pre><code class="language-bash">$ sudo yum remove docker-ce
</code></pre>
<p><a name="q1ht7"></a></p>
<h3 id="删除本地文件">删除本地文件</h3>
<p><br />注意，docker 的本地文件，包括镜像(images), 容器(containers), 存储卷(volumes)等，都需要手工删除。默认目录存储在 <code>/var/lib/docker</code>。<br /></p>
<pre><code class="language-bash">$ sudo rm -rf /var/lib/docker
</code></pre>
<p><a name="RDRS3"></a></p>
<h2 id="结论">结论</h2>
<p><br />本文演示了 CentOS 7 下 yum 安装 Docker CE 的步骤。<br>
<a name="JIh33"></a></p>
<h2 id="参考资料">参考资料</h2>
<p><br /><a href="https://docs.docker.com/install/linux/docker-ce/centos/">Get Docker CE for CentOS</a><br /><a href="https://wiki.centos.org/AdditionalResources/Repositories">Available Repositories for CentOS</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring @Value用法小结，#与$的区别]]></title>
        <id>https://blockmao.github.io/OQBr_T227/</id>
        <link href="https://blockmao.github.io/OQBr_T227/">
        </link>
        <updated>2020-04-11T07:03:46.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>原文地址：<a href="https://www.cnblogs.com/larryzeal/p/5910149.html">https://www.cnblogs.com/larryzeal/p/5910149.html</a></p>
</blockquote>
<h2 id="起因">起因</h2>
<p><br />一直的用法是 <code>@Value(&quot;${jdbc.driverClass}&quot;)</code>这样，但在Spring官方文档里又看到 <code>@Value(&quot;#{a.b}&quot;)</code>的用法。<br /></p>
<p><a name="54bbba80"></a></p>
<h2 id="结论">结论</h2>
<p><br />@Value的值有两类：<br /></p>
<ul>
<li><code>${ property : default_value }</code>：注入的是外部参数对应的property</li>
<li><code>#{ obj.property? : default_value }</code>：SpEL表达式对应的内容</li>
</ul>
<p><br />那个 <code>default_value</code>，就是前面的值为空时的默认值。注意二者的不同。<br /></p>
<p><a name="8cb94eb1"></a></p>
<h2 id="例子">例子</h2>
<p>1、使用STS 新建一个Spring Boot项目，依赖部分留空。<br>
2、在application.properties 文件中添加如下内容</p>
<pre><code class="language-properties">jdbc.driverClass=com.mysql
jdbc.url=3306@local
jdbc.user=admin
jdbc.pwd=pwd
</code></pre>
<p><br />3、新建类ValueDemo，内容如下：<br /></p>
<pre><code class="language-java">package cn.larry.spring;

import javax.annotation.PostConstruct;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class ValueDemo {
    @Value(&quot;${jdbc.driverClass}&quot;)
    private String driver;
    
    @PostConstruct
    public void run(){
        System.out.println(driver);
    }

}
</code></pre>
<p><br />4、启动Spring Boot项目，输出如下：<br /></p>
<figure data-type="image" tabindex="1"><img src="https://blockmao.github.io/post-images/1586589021505.png" alt="" loading="lazy"></figure>
<blockquote>
<p>这就是常用的 <code>${properties}</code>，很简单。继续。</p>
</blockquote>
<p><br />5、再新建一个类AnotherObj，内容如下：<br /></p>
<pre><code class="language-java">package cn.larry.spring;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class AnotherObj {
    @Value(&quot;${jdbc.user}&quot;)
    private String name;
    @Value(&quot;${jdbc.pwd}&quot;)
    private String pwd;

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getPwd() {
        return pwd;
    }

    public void setPwd(String pwd) {
        this.pwd = pwd;
    }

}
</code></pre>
<p><br />6、修改ValueDemo，如下：<br /></p>
<pre><code class="language-java">package cn.larry.spring;

import javax.annotation.PostConstruct;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class ValueDemo {
    @Value(&quot;${jdbc.driverClass}&quot;)
    private String driver;
    
    @Value(&quot;#{anotherObj.name}&quot;)
    private String name;
    
    @PostConstruct
    public void run(){
        System.out.println(driver);
        System.out.println(name);
    }
    
}
</code></pre>
<p>7、再次执行Spring Boot项目，结果如下：</p>
<figure data-type="image" tabindex="2"><img src="https://blockmao.github.io/post-images/1586589066868.png" alt="" loading="lazy"></figure>
<p>这里展示的是 <code>#{ SpEL }</code>的效果。</p>
<p>8、有意思的是，二者可以结合使用，如：<code>#{ '${}' }</code> ，注意单引号，注意不能反过来，测试如下。</p>
<p>application.properties文件中添加一行：<code>media=jdbc.url</code></p>
<p>ValueDemo类修改如下：</p>
<pre><code class="language-java">package cn.larry.spring;

import javax.annotation.PostConstruct;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class ValueDemo {
    @Value(&quot;${jdbc.driverClass}&quot;)
    private String driver;
    
    @Value(&quot;#{anotherObj.name}&quot;)
    private String name;
    
//    @Value(&quot;${ '#{anotherObj.media}' }&quot;) //这个不支持。
    @Value(&quot;#{ '${media}' }&quot;)
    private String media;
    
    @PostConstruct
    public void run(){
        System.out.println(driver);
        System.out.println(name);
        System.out.println(media);
    }
    
}
</code></pre>
<p><br />9、执行Spring Boot项目，结果如下：<br /></p>
<figure data-type="image" tabindex="3"><img src="https://blockmao.github.io/post-images/1586589095169.png" alt="" loading="lazy"></figure>
<p><br />另外，默认值很浅显，也不再测试了。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[动态修改Java日志级别]]></title>
        <id>https://blockmao.github.io/dong-tai-xiu-gai-java-ri-zhi-ji-bie/</id>
        <link href="https://blockmao.github.io/dong-tai-xiu-gai-java-ri-zhi-ji-bie/">
        </link>
        <updated>2019-11-09T05:18:27.000Z</updated>
        <summary type="html"><![CDATA[<p>作为开发人员，定位问题是我们的日常工作，而日志是我们定位问题非常重要的依据。传统方式定位问题时，往往是如下步骤：</p>
<ol>
<li>将日志级别设低，例如 DEBUG ；</li>
<li>重启应用；</li>
<li>复现问题，观察日志；</li>
</ol>
<p>实际上是可以动态修改日志级别，无需重启应用，立即生效。</p>
]]></summary>
        <content type="html"><![CDATA[<p>作为开发人员，定位问题是我们的日常工作，而日志是我们定位问题非常重要的依据。传统方式定位问题时，往往是如下步骤：</p>
<ol>
<li>将日志级别设低，例如 DEBUG ；</li>
<li>重启应用；</li>
<li>复现问题，观察日志；</li>
</ol>
<p>实际上是可以动态修改日志级别，无需重启应用，立即生效。</p>
<!-- more -->
<p>本文收集了3种动态修改日志级别的文章，分别是</p>
<ol>
<li><a href="http://www.itmuch.com/spring-boot/change-logger-level/">Spring Boot 2动态修改日志级别</a></li>
<li><a href="https://blog.csdn.net/yusimiao/article/details/95032376">阿里在线诊断工具Arthas调整日志等级记录</a></li>
<li><a href="https://tech.meituan.com/2017/02/17/change-log-level.html">美团日志级别动态调整——小工具解决大问题</a></li>
</ol>
<h2 id="spirng-boot动态修改日志级别">Spirng Boot动态修改日志级别</h2>
<p>从 Spring Boot 1.5 开始，Spring Boot Actuator 组件就已提供动态修改日志级别的能力。</p>
<h3 id="示例">示例</h3>
<p>1.引入<code>spring-boot-starter-actuator</code>依赖，内容如下：</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-actuator&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p>2.编写测试代码，如下：</p>
<pre><code class="language-java">@SpringBootApplication
public class DemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }
}

@RestController
public class DemoController {

    private static Logger logger = LoggerFactory.getLogger(DemoController.class);

    @GetMapping(&quot;/helloworld&quot;)
    public String helloworld(){
        logger.debug(&quot;welcome to learn spring boot&quot;);
        return &quot;welcome to learn spring boot&quot;;
    }

}
</code></pre>
<p>3.配置文件</p>
<pre><code class="language-yaml">management:
  endpoints:
    web:
      exposure:
        include: 'loggers'
</code></pre>
<blockquote>
<p>Spring Boot 2.x默认只暴露 /health 以及 /info 端点，而日志控制需要用到 /loggers 端点，故而需要设置将其暴露。</p>
</blockquote>
<h3 id="测试">测试</h3>
<p><code>/loggers</code>端点提供了查看以及修改日志级别的能力。</p>
<ol>
<li>
<p>查看当前应用各包/类的日志级别<br>
访问 http://localhost:8080/actuator/loggers ，可看到类似如下的结果：</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a715b57a71?w=1080&amp;h=1454&amp;f=png&amp;s=204826" alt="" loading="lazy"></figure>
</li>
<li>
<p>查看指定包/类日志详情<br>
访问 http://localhost:8080/actuator/loggers/com.blockmao.springboot.demo.DemoController ，可看到类似如下的结果：</p>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a715a0369e?w=1558&amp;h=274&amp;f=png&amp;s=49012" alt="" loading="lazy"></figure>
</li>
<li>
<p>修改日志级别<br>
默认的日志级别是INFO，所以<code>DemoController</code>的debug日志不会打印。下面来尝试将该类的日志级别设为<code>DEBUG</code>，如下</p>
<figure data-type="image" tabindex="3"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a71a69dcbf?w=1680&amp;h=408&amp;f=png&amp;s=71269" alt="" loading="lazy"></figure>
<p>此时，访问 http://localhost:8080/helloworld 会看到类似如下的日志：</p>
<figure data-type="image" tabindex="4"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a71c7e3011?w=1860&amp;h=172&amp;f=png&amp;s=40951" alt="" loading="lazy"></figure>
<p>并且，此时再访问 http://localhost:8080/actuator/loggers/com.itmuch.logging.TestController ，可看到类似如下的结果：</p>
<figure data-type="image" tabindex="5"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a71c897bfb?w=1430&amp;h=280&amp;f=png&amp;s=49437" alt="" loading="lazy"></figure>
</li>
</ol>
<h3 id="原理">原理</h3>
<p><strong>Actuator</strong>有约定，<code>/actuator/xxx</code>端点的定义代码在 <code>xxxEndpoint</code>中。找到类<code>org.springframework.boot.actuate.logging.LoggersEndpoint</code>，代码如下：</p>
<pre><code class="language-java">@Endpoint(id = &quot;loggers&quot;)
public class LoggersEndpoint {
	private final LoggingSystem loggingSystem;

	@WriteOperation
	public void configureLogLevel(@Selector String name,
			@Nullable LogLevel configuredLevel) {
		Assert.notNull(name, &quot;Name must not be empty&quot;);
		this.loggingSystem.setLogLevel(name, configuredLevel);
	}
	// ...其他省略
}
</code></pre>
<blockquote>
<p>其中， <code>Endpoint</code> 、<code>WriteOperation</code> 、<code>@Selector</code>都是Spring Boot 2.0开始提供的新注解。</p>
</blockquote>
<p><code>@Endpoint(id = &quot;loggers&quot;)</code>用来描述Spring Boot Actuator 的端点，这样就会产生一个<code>/actuator/loggers</code>的路径，它类似于Spring MVC的<code>@RequestMapping(&quot;loggers&quot;)</code>。</p>
<p>@WriteOperation 表示这是一个写操作，它类似于Spring MVC的 @PostMapping 。Spring Boot Actuator还提供了其他操作，如下表：</p>
<table>
<thead>
<tr>
<th>Operation</th>
<th>HTTP method</th>
</tr>
</thead>
<tbody>
<tr>
<td>@ReadOperation</td>
<td>GET</td>
</tr>
<tr>
<td>@WriteOperation</td>
<td>POST</td>
</tr>
<tr>
<td>@DeleteOperation</td>
<td>DELETE</td>
</tr>
</tbody>
</table>
<p>@Selector用于筛选@Endpoint注解返回值的子集，它类似于Spring MVC的@PathVariable 。</p>
<p>这样，上面的代码就很好理解了— <code>configureLogLevel</code> 方法：送POST请求后，name就是我们传的包名或者类名，configuredLevel就是我们传的消息体。</p>
<p><code>org.springframework.boot.logging.LoggingSystem#setLogLevel</code>是抽象方法，具体实现由子类完成。<code>LoggingSystem</code>类结构如下图所示：</p>
<figure data-type="image" tabindex="6"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a71b661bba?w=1242&amp;h=658&amp;f=png&amp;s=55177" alt="" loading="lazy"></figure>
<p>LoggingSystem有这么多实现类，Spring Boot怎么知道什么情况下用什么LoggingSystem呢？可在 <code>org.springframework.boot.logging.LoggingSystem</code> 找到类似如下代码：</p>
<pre><code class="language-java">public abstract class LoggingSystem {
	private static final Map&lt;String, String&gt; SYSTEMS;

	static {
		Map&lt;String, String&gt; systems = new LinkedHashMap&lt;&gt;();
		systems.put(&quot;ch.qos.logback.core.Appender&quot;,
				&quot;org.springframework.boot.logging.logback.LogbackLoggingSystem&quot;);
		systems.put(&quot;org.apache.logging.log4j.core.impl.Log4jContextFactory&quot;,
				&quot;org.springframework.boot.logging.log4j2.Log4J2LoggingSystem&quot;);
		systems.put(&quot;java.util.logging.LogManager&quot;,
				&quot;org.springframework.boot.logging.java.JavaLoggingSystem&quot;);
		SYSTEMS = Collections.unmodifiableMap(systems);
	}

	/**
	 * Detect and return the logging system in use. Supports Logback and Java Logging.
	 * @param classLoader the classloader
	 * @return the logging system
	 */
	public static LoggingSystem get(ClassLoader classLoader) {
		String loggingSystem = System.getProperty(SYSTEM_PROPERTY);
		if (StringUtils.hasLength(loggingSystem)) {
			if (NONE.equals(loggingSystem)) {
				return new NoOpLoggingSystem();
			}
			return get(classLoader, loggingSystem);
		}
		return SYSTEMS.entrySet().stream()
				.filter((entry) -&gt; ClassUtils.isPresent(entry.getKey(), classLoader))
				.map((entry) -&gt; get(classLoader, entry.getValue())).findFirst()
				.orElseThrow(() -&gt; new IllegalStateException(
						&quot;No suitable logging system located&quot;));
	}
  // 省略不相关内容...
}
</code></pre>
<p>由代码不难发现，其实就是构建了一个名为 SYSTEMS 的map，作为各种日志系统的字典；然后在 get 方法中，看应用是否加载了map中的类；如果加载了，就通过反射，初始化<code>LoggingSystem</code> 。例如：Spring Boot发现当前应用加载了<code>ch.qos.logback.core.Appender</code> ，就去实例化 <code>org.springframework.boot.logging.logback.LogbackLoggingSystem</code>。</p>
<h2 id="arthas-ognl命令动态修改日志级别">Arthas ognl命令动态修改日志级别</h2>
<p>使用<code>ognl</code>命令可以动态修改日志级别，步骤如下：</p>
<ol>
<li>
<p>查找当前类的classLoaderHash</p>
<figure data-type="image" tabindex="7"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1b4c0dda963?w=1510&amp;h=78&amp;f=png&amp;s=54236" alt="" loading="lazy"></figure>
</li>
<li>
<p>用OGNL获取logger</p>
<figure data-type="image" tabindex="8"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a91c9c634d?w=1446&amp;h=476&amp;f=png&amp;s=252162" alt="" loading="lazy"></figure>
<blockquote>
<p>可以发现日志使用的是Logback框架。</p>
</blockquote>
</li>
<li>
<p>单独设置DemoController的logger level</p>
<figure data-type="image" tabindex="9"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1a9b6aa8a2a?w=2082&amp;h=1042&amp;f=png&amp;s=597082" alt="" loading="lazy"></figure>
</li>
<li>
<p>全局设置logger level</p>
<figure data-type="image" tabindex="10"><img src="https://user-gold-cdn.xitu.io/2019/10/9/16dac1aa098223fa?w=2030&amp;h=1052&amp;f=png&amp;s=592302" alt="" loading="lazy"></figure>
</li>
</ol>
<blockquote>
<p>如果使用的日志框架是<code>log4j</code>，则使用上述<code>ognl</code>命令则会报错。至于为什么？请阅读<a href="https://blog.blockmao.cn/2019/09/21/Java%E6%97%A5%E5%BF%97%E6%A1%86%E6%9E%B6%EF%BC%9ASLF4J%E8%AF%A6%E8%A7%A3/#more">Java日志：SLF4J详解</a></p>
</blockquote>
<h2 id="美团日志级别动态调整小工具">美团日志级别动态调整小工具</h2>
<ol>
<li>
<p>初始化：确定所使用的日志框架，获取配置文件中所有的Logger内存实例，并将它们的引用缓存到Map容器中。</p>
<pre><code class="language-java">String type = StaticLoggerBinder.getSingleton().getLoggerFactoryClassStr();
if (LogConstant.LOG4J_LOGGER_FACTORY.equals(type)) {
    logFrameworkType = LogFrameworkType.LOG4J;
    Enumeration enumeration = org.apache.log4j.LogManager.getCurrentLoggers();
    while (enumeration.hasMoreElements()) {
        org.apache.log4j.Logger logger = (org.apache.log4j.Logger) enumeration.nextElement();
        if (logger.getLevel() != null) {
            loggerMap.put(logger.getName(), logger);
        }
    }
    org.apache.log4j.Logger rootLogger = org.apache.log4j.LogManager.getRootLogger();
    loggerMap.put(rootLogger.getName(), rootLogger);
} else if (LogConstant.LOGBACK_LOGGER_FACTORY.equals(type)) {
    logFrameworkType = LogFrameworkType.LOGBACK;
    ch.qos.logback.classic.LoggerContext loggerContext = (ch.qos.logback.classic.LoggerContext) LoggerFactory.getILoggerFactory();
    for (ch.qos.logback.classic.Logger logger : loggerContext.getLoggerList()) {
        if (logger.getLevel() != null) {
            loggerMap.put(logger.getName(), logger);
        }
    }
    ch.qos.logback.classic.Logger rootLogger = (ch.qos.logback.classic.Logger) LoggerFactory.getLogger(Logger.ROOT_LOGGER_NAME);
    loggerMap.put(rootLogger.getName(), rootLogger);
} else if (LogConstant.LOG4J2_LOGGER_FACTORY.equals(type)) {
    logFrameworkType = LogFrameworkType.LOG4J2;
    org.apache.logging.log4j.core.LoggerContext loggerContext = (org.apache.logging.log4j.core.LoggerContext) org.apache.logging.log4j.LogManager.getContext(false);
    Map&lt;String, org.apache.logging.log4j.core.config.LoggerConfig&gt; map = loggerContext.getConfiguration().getLoggers();
    for (org.apache.logging.log4j.core.config.LoggerConfig loggerConfig : map.values()) {
        String key = loggerConfig.getName();
        if (StringUtils.isBlank(key)) {
            key = &quot;root&quot;;
        }
        loggerMap.put(key, loggerConfig);
    }
} else {
    logFrameworkType = LogFrameworkType.UNKNOWN;
    LOG.error(&quot;Log框架无法识别: type={}&quot;, type);
}
</code></pre>
</li>
<li>
<p>获取Logger列表：从本地Map容器取出。</p>
<pre><code class="language-java">private String getLoggerList() {
    JSONObject result = new JSONObject();
    result.put(&quot;logFramework&quot;, logFrameworkType);
    JSONArray loggerList = new JSONArray();
    for (ConcurrentMap.Entry&lt;String, Object&gt; entry : loggerMap.entrySet()) {
        JSONObject loggerJSON = new JSONObject();
        loggerJSON.put(&quot;loggerName&quot;, entry.getKey());
        if (logFrameworkType == LogFrameworkType.LOG4J) {
            org.apache.log4j.Logger targetLogger = (org.apache.log4j.Logger) entry.getValue();
            loggerJSON.put(&quot;logLevel&quot;, targetLogger.getLevel().toString());
        } else if (logFrameworkType == LogFrameworkType.LOGBACK) {
            ch.qos.logback.classic.Logger targetLogger = (ch.qos.logback.classic.Logger) entry.getValue();
            loggerJSON.put(&quot;logLevel&quot;, targetLogger.getLevel().toString());
        } else if (logFrameworkType == LogFrameworkType.LOG4J2) {
            org.apache.logging.log4j.core.config.LoggerConfig targetLogger = (org.apache.logging.log4j.core.config.LoggerConfig) entry.getValue();
            loggerJSON.put(&quot;logLevel&quot;, targetLogger.getLevel().toString());
        } else {
            loggerJSON.put(&quot;logLevel&quot;, &quot;Logger的类型未知,无法处理!&quot;);
        }
        loggerList.add(loggerJSON);
    }
    result.put(&quot;loggerList&quot;, loggerList);
    LOG.info(&quot;getLoggerList: result={}&quot;, result.toString());
    return result.toString();
}
</code></pre>
</li>
<li>
<p>修改Logger的级别</p>
<pre><code class="language-java">private String setLogLevel(JSONArray data) {
    LOG.info(&quot;setLogLevel: data={}&quot;, data);
    List&lt;LoggerBean&gt; loggerList = parseJsonData(data);
    if (CollectionUtils.isEmpty(loggerList)) {
        return &quot;&quot;;
    }
    for (LoggerBean loggerbean : loggerList) {
        Object logger = loggerMap.get(loggerbean.getName());
        if (logger == null) {
            throw new RuntimeException(&quot;需要修改日志级别的Logger不存在&quot;);
        }
        if (logFrameworkType == LogFrameworkType.LOG4J) {
            org.apache.log4j.Logger targetLogger = (org.apache.log4j.Logger) logger;
            org.apache.log4j.Level targetLevel = org.apache.log4j.Level.toLevel(loggerbean.getLevel());
            targetLogger.setLevel(targetLevel);
        } else if (logFrameworkType == LogFrameworkType.LOGBACK) {
            ch.qos.logback.classic.Logger targetLogger = (ch.qos.logback.classic.Logger) logger;
            ch.qos.logback.classic.Level targetLevel = ch.qos.logback.classic.Level.toLevel(loggerbean.getLevel());
            targetLogger.setLevel(targetLevel);
        } else if (logFrameworkType == LogFrameworkType.LOG4J2) {
            org.apache.logging.log4j.core.config.LoggerConfig loggerConfig = (org.apache.logging.log4j.core.config.LoggerConfig) logger;
            org.apache.logging.log4j.Level targetLevel = org.apache.logging.log4j.Level.toLevel(loggerbean.getLevel());
            loggerConfig.setLevel(targetLevel);
            org.apache.logging.log4j.core.LoggerContext ctx = (org.apache.logging.log4j.core.LoggerContext) org.apache.logging.log4j.LogManager.getContext(false);
            ctx.updateLoggers(); // This causes all Loggers to refetch information from their LoggerConfig.
        } else {
            throw new RuntimeException(&quot;Logger的类型未知,无法处理!&quot;);
        }
    }
    return &quot;success&quot;;
}
</code></pre>
</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SLF4J详解]]></title>
        <id>https://blockmao.github.io/UwO0NFpFm/</id>
        <link href="https://blockmao.github.io/UwO0NFpFm/">
        </link>
        <updated>2019-11-01T06:03:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="slf4j概述">SLF4J概述</h2>
<p>The Simple Logging Facade for Java (SLF4J) serves as a simple facade or abstraction for various logging frameworks, such as java.util.logging, logback and log4j. SLF4J allows the end-user to plug in the desired logging framework at deployment time.</p>
<blockquote>
<p><code>Simple Logging Facade for Java</code>（SLF4J）可用作各种日志框架的简单外观或抽象，例如<code>java.util.logging</code>，<code>logback</code>和<code>log4j</code>。<code>SLF4J</code>允许最终用户在部署时插入所需的日志记录框架。</p>
</blockquote>
<h3 id="binding-with-a-logging-framework-at-deployment-time">Binding with a logging framework at deployment time</h3>
<p>The <code>SLF4J</code> distribution ships with several jar files referred to as <code>SLF4J bindings</code>, with each binding corresponding to a supported framework.</p>
<ul>
<li>
<p><strong>slf4j-log4j12-1.7.27.jar</strong></p>
<p>Binding for <code>log4j version 1.2</code>, a widely used logging framework. You also need to place <code>log4j.jar</code> on your class path.</p>
</li>
<li>
<p><strong>slf4j-jdk14-1.7.27.jar</strong></p>
<p>Binding for <code>java.util.logging</code>, also referred to as JDK 1.4 logging</p>
</li>
<li>
<p><strong>slf4j-nop-1.7.27.jar</strong></p>
<p>Binding for NOP, silently discarding all logging.</p>
</li>
<li>
<p><strong>slf4j-simple-1.7.27.jar</strong></p>
<p>Binding for Simple implementation, which outputs all events to System.err. Only messages of level INFO and higher are printed. This binding may be useful in the context of small applications.</p>
</li>
<li>
<p><strong>slf4j-jcl-1.7.27.jar</strong></p>
<p>Binding for <code>Jakarta Commons Logging</code>. This binding will delegate all SLF4J logging to JCL.</p>
</li>
<li>
<p><strong>logback-classic-1.2.3.jar (requires logback-core-1.2.3.jar)</strong></p>
<p>There are also SLF4J bindings external to the SLF4J project, <code>Logback's ch.qos.logback.classic.Logger</code>class is a direct implementation of SLF4J's <code>org.slf4j.Logger</code> interface.</p>
</li>
</ul>
<p>To switch logging frameworks, just replace slf4j bindings on your class path. For example, to switch from java.util.logging to log4j, just replace slf4j-jdk14-1.7.27.jar with slf4j-log4j12-1.7.27.jar.</p>
<blockquote>
<p>要切换日志框架，只需替换类路径上的slf4j绑定。例如，要从java.util.logging切换到log4j，只需将slf4j-jdk14-1.7.27.jar替换为slf4j-log4j12-1.7.27.jar即可。</p>
</blockquote>
<p><strong>SLF4J does not rely on any special class loader machinery. In fact, each SLF4J binding is hardwired at compile time to use one and only one specific logging framework</strong>. For example, the slf4j-log4j12-1.7.27.jar binding is bound at compile time to use log4j. In your code, in addition to slf4j-api-1.7.27.jar, you simply drop one and only one binding of your choice onto the appropriate class path location. Do not place more than one binding on your class path.</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d5229391b1665b?w=1152&amp;h=636&amp;f=png&amp;s=82592" alt="" loading="lazy"></figure>
<h3 id="consolidate-logging-via-slf4j通过slf4j整合日志记录">Consolidate logging via SLF4J（通过SLF4J整合日志记录）</h3>
<p>SLF4J caters for this common use-case by providing bridging modules for JCL, java.util.logging and log4j.</p>
<blockquote>
<p>SLF4J通过为JCL，java.util.logging和log4j提供桥接模块来满足这种常见用例。</p>
</blockquote>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d52293d0832281?w=1587&amp;h=1123&amp;f=png&amp;s=180307" alt="" loading="lazy"></figure>
<h3 id="mapped-diagnostic-context-mdc-support">Mapped Diagnostic Context (MDC) support</h3>
<p><code>Mapped Diagnostic Context</code> is essentially a map maintained by the logging framework where the application code provides key-value pairs which can then be inserted by the logging framework in log messages. <code>MDC</code> data can also be highly helpful in filtering messages or triggering certain actions.</p>
<blockquote>
<p>映射诊断上下文本质上是由日志框架维护的映射，其中应用程序代码提供键值对，然后日志消息中的日志记录框架可以插入键值对。 MDC数据在过滤消息或触发某些操作方面也非常有用。</p>
</blockquote>
<p><code>SLF4J</code> supports <code>MDC</code>, or mapped diagnostic context. If the underlying logging framework offers <code>MDC</code> functionality, then <code>SLF4J</code> will delegate to the underlying framework's MDC. Note that at this time, only <code>log4j</code> and <code>logback</code> offer <code>MDC</code> functionality. If the underlying framework does not offer <code>MDC</code>, for example <code>java.util.logging</code>, then <code>SLF4J</code> will still store <code>MDC</code> data but the information therein will need to be retrieved by custom user code.</p>
<blockquote>
<p>SLF4J支持MDC或映射诊断上下文。 如果底层日志记录框架提供了MDC功能，那么SLF4J将委托给底层框架的MDC。 请注意，目前只有log4j和logback提供MDC功能。 如果底层框架不提供MDC，例如java.util.logging，则SLF4J仍将存储MDC数据，但其中的信息需要由自定义用户代码检索。</p>
</blockquote>
<h2 id="原理分析">原理分析</h2>
<figure data-type="image" tabindex="3"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d52292a208942d?w=1606&amp;h=798&amp;f=png&amp;s=71389" alt="" loading="lazy"></figure>
<p>通过上图可知<code>SLF4J</code>能够抽象各种具体日志框架，是由<code>StaticLoggerBinder</code>类完成。接下来重点看看<code>bind()</code>方法是如何查找<code>StaticLoggerBinder</code>类，源码如下：</p>
<pre><code class="language-java">private final static void bind() {
    try {
        // 查找类路径下所有的StaticLoggerBinder类
        Set&lt;URL&gt; staticLoggerBinderPathSet = findPossibleStaticLoggerBinderPathSet();
        // 如果存在多个StaticLoggerBinder类，则打印日志
        reportMultipleBindingAmbiguity(staticLoggerBinderPathSet);
        // 获取StaticLoggerBinder实例，如果不存在，则抛出NoClassDefFoundError异常
        StaticLoggerBinder.getSingleton();
        INITIALIZATION_STATE = SUCCESSFUL_INITIALIZATION;
        // 打印实际使用StaticLoggerBinder类
        reportActualBinding(staticLoggerBinderPathSet);
        fixSubstitutedLoggers();
    } catch (NoClassDefFoundError ncde) {
        String msg = ncde.getMessage();
        if (messageContainsOrgSlf4jImplStaticLoggerBinder(msg)) {
            INITIALIZATION_STATE = NOP_FALLBACK_INITIALIZATION;
            Util.report(&quot;Failed to load class \&quot;org.slf4j.impl.StaticLoggerBinder\&quot;.&quot;);
            Util.report(&quot;Defaulting to no-operation (NOP) logger implementation&quot;);
            Util.report(&quot;See &quot; + NO_STATICLOGGERBINDER_URL + &quot; for further details.&quot;);
        } else {
            failedBinding(ncde);
            throw ncde;
        }
    } catch (java.lang.NoSuchMethodError nsme) {
        String msg = nsme.getMessage();
        if (msg != null &amp;&amp; msg.indexOf(&quot;org.slf4j.impl.StaticLoggerBinder.getSingleton()&quot;) != -1) {
            INITIALIZATION_STATE = FAILED_INITIALIZATION;
            Util.report(&quot;slf4j-api 1.6.x (or later) is incompatible with this binding.&quot;);
            Util.report(&quot;Your binding is version 1.5.5 or earlier.&quot;);
            Util.report(&quot;Upgrade your binding to version 1.6.x.&quot;);
        }
        throw nsme;
    } catch (Exception e) {
        failedBinding(e);
        throw new IllegalStateException(&quot;Unexpected initialization failure&quot;, e);
    }
}

private static String  STATIC_LOGGER_BINDER_PATH = &quot;org/slf4j/impl/StaticLoggerBinder.class&quot;;

private static Set&lt;URL&gt; findPossibleStaticLoggerBinderPathSet() {
    Set&lt;URL&gt; staticLoggerBinderPathSet = new LinkedHashSet&lt;URL&gt;();
    try {
        ClassLoader loggerFactoryClassLoader = LoggerFactory.class.getClassLoader();
        Enumeration&lt;URL&gt; paths;
        if (loggerFactoryClassLoader == null) {
            paths = ClassLoader.getSystemResources(STATIC_LOGGER_BINDER_PATH);
        } else {
            paths = loggerFactoryClassLoader.getResources(STATIC_LOGGER_BINDER_PATH);
        }
        while (paths.hasMoreElements()) {
            URL path = (URL) paths.nextElement();
            staticLoggerBinderPathSet.add(path);
        }
    } catch (IOException ioe) {
        Util.report(&quot;Error getting resources from path&quot;, ioe);
    }
    return staticLoggerBinderPathSet;
}
</code></pre>
<h2 id="slf4j与logback集成">SLF4J与logback集成</h2>
<ol>
<li>
<p>maven依赖包如下：</p>
<pre><code class="language-xml">&lt;!-- slf4j-api --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.slf4j&lt;/groupId&gt;
    &lt;artifactId&gt;slf4j-api&lt;/artifactId&gt;
    &lt;version&gt;1.7.12&lt;/version&gt;
&lt;/dependency&gt;
&lt;!-- logback --&gt;
&lt;dependency&gt; 
    &lt;groupId&gt;ch.qos.logback&lt;/groupId&gt; 
    &lt;artifactId&gt;logback-core&lt;/artifactId&gt; 
    &lt;version&gt;1.1.3&lt;/version&gt; 
&lt;/dependency&gt;
&lt;!-- logback-classic（已含有对slf4j的集成包） --&gt; 
&lt;dependency&gt; 
    &lt;groupId&gt;ch.qos.logback&lt;/groupId&gt; 
    &lt;artifactId&gt;logback-classic&lt;/artifactId&gt; 
    &lt;version&gt;1.1.3&lt;/version&gt; 
&lt;/dependency&gt;
</code></pre>
</li>
<li>
<p>编写logback的配置文件logback.xml，内容如下:</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;configuration&gt;
  &lt;appender name=&quot;STDOUT&quot; class=&quot;ch.qos.logback.core.ConsoleAppender&quot;&gt;
    &lt;encoder&gt;
      &lt;pattern&gt;%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n&lt;/pattern&gt;
    &lt;/encoder&gt;
  &lt;/appender&gt;
  &lt;root level=&quot;DEBUG&quot;&gt;          
    &lt;appender-ref ref=&quot;STDOUT&quot; /&gt;
  &lt;/root&gt;  
&lt;/configuration&gt;
</code></pre>
</li>
<li>
<p>使用方式</p>
<pre><code class="language-java">private static final Logger logger=LoggerFactory.getLogger(LogbackTest.class);
public static void main(String[] args){
    if(logger.isDebugEnabled()){
        logger.debug(&quot;slf4j-logback debug message&quot;);
    }
    if(logger.isInfoEnabled()){
        logger.info(&quot;slf4j-logback info message&quot;);
    }
    if(logger.isTraceEnabled()){
        logger.trace(&quot;slf4j-logback trace message&quot;);
    }
}
</code></pre>
</li>
<li>
<p><code>org.slf4j.impl.StaticLoggerBinder</code>类实现</p>
<figure data-type="image" tabindex="4"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d5229f496aaaf8?w=850&amp;h=394&amp;f=png&amp;s=68466" alt="" loading="lazy"></figure>
<hr>
<figure data-type="image" tabindex="5"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d5229f49549aa2?w=1332&amp;h=350&amp;f=png&amp;s=42439" alt="" loading="lazy"></figure>
</li>
</ol>
<h2 id="slf4j与log4集成">SLF4J与log4集成</h2>
<ol>
<li>
<p>maven依赖包如下：</p>
<pre><code class="language-xml">&lt;!-- slf4j --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.slf4j&lt;/groupId&gt;
    &lt;artifactId&gt;slf4j-api&lt;/artifactId&gt;
    &lt;version&gt;1.7.12&lt;/version&gt;
&lt;/dependency&gt;

&lt;!-- slf4j-log4j --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.slf4j&lt;/groupId&gt;
    &lt;artifactId&gt;slf4j-log4j12&lt;/artifactId&gt;
    &lt;version&gt;1.7.12&lt;/version&gt;
&lt;/dependency&gt;

&lt;!-- log4j --&gt;
&lt;dependency&gt;
    &lt;groupId&gt;log4j&lt;/groupId&gt;
    &lt;artifactId&gt;log4j&lt;/artifactId&gt;
    &lt;version&gt;1.2.17&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
</li>
<li>
<p>编写log4j.properties配置文件,放到类路径下</p>
<pre><code class="language-properties">log4j.rootLogger = debug, console
log4j.appender.console = org.apache.log4j.ConsoleAppender
log4j.appender.console.layout = org.apache.log4j.PatternLayout
log4j.appender.console.layout.ConversionPattern = %-d{yyyy-MM-dd HH:mm:ss} %m%n
</code></pre>
</li>
<li>
<p>使用方式</p>
<pre><code class="language-java">private static Logger logger=LoggerFactory.getLogger(Log4j2Slf4jTest.class);
public static void main(String[] args){
    if(logger.isTraceEnabled()){
        logger.trace(&quot;slf4j-log4j2 trace message&quot;);
    }
    if(logger.isDebugEnabled()){
        logger.debug(&quot;slf4j-log4j2 debug message&quot;);
    }
    if(logger.isInfoEnabled()){
        logger.info(&quot;slf4j-log4j2 info message&quot;);
    }
}
</code></pre>
</li>
<li>
<p><code>org.slf4j.impl.StaticLoggerBinder</code>类实现</p>
<figure data-type="image" tabindex="6"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d522ab91f86aa7?w=1204&amp;h=510&amp;f=png&amp;s=124354" alt="" loading="lazy"></figure>
<hr>
<figure data-type="image" tabindex="7"><img src="https://user-gold-cdn.xitu.io/2019/9/21/16d5229f49bae156?w=1374&amp;h=470&amp;f=png&amp;s=60777" alt="" loading="lazy"></figure>
</li>
</ol>
<h2 id="参考资料">参考资料</h2>
<p><a href="https://my.oschina.net/xianggao/blog/515996">jcl与jul、log4j1、log4j2、logback的集成原理</a></p>
]]></content>
    </entry>
</feed>